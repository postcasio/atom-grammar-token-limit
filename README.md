# grammar-token-limit package

Change Atom's token limit for syntax highlighting.

## Warning

Atom limits the number of tokens that will be syntax highlighted on a line for performance reasons. Be careful when increasing this limit.
